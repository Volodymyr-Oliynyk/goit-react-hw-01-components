import styled from 'styled-components';

export const Section = styled.section`
width: max-content;
  margin: 20px auto;
  padding: 20px 50px;
  background-color: ${props => props.theme.colors.backgroundSections};
  border-radius: 5px;
  `;
